{
  "name": "sistem-reservasi-aset",
  "version": "1.0.0",
  "description": "Sistem Informasi Reservasi Fasilitas Terintegrasi",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
